---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    symbiotic:
      hosts:
        kalypso.symbiotic.mainnet.encapsulate.xyz:
          type: kalypso
