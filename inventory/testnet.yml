---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    symbiotic:
      hosts:
        kalypso.symbiotic.testnet.encapsulate.xyz:
          type: kalypso
